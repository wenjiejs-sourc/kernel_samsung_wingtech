name: Build Samsung A04 SukiSU-Ultra

on:
  workflow_dispatch: # 手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 检出仓库代码
        uses: actions/checkout@v4
        with:
          submodules: true # 如果有子模块则一并检出

      - name: 安装构建环境
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential flex bison g++-multilib gcc-multilib \
          binutils-aarch64-linux-gnu git gnupg gperf lib32ncurses5-dev lib32readline-dev \
          lib32z1-dev liblz4-tool libncurses5-dev libsdl1.2-dev libssl-dev libxml2 \
          libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev \
          python3 python2

      - name: 获取工具链 (AOSP Clang)
        run: |
          mkdir clang
          wget https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/master/clang-r416183b1.tar.gz
          tar -C clang/ -xf clang-r416183b1.tar.gz

      - name: 集成 SukiSU-Ultra
        run: |
          # 直接在当前目录运行集成脚本
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s main
          
          # 根据你的仓库，A04 的 defconfig 通常是 a04_global_defconfig 或 a04_defconfig
          # 脚本会自动尝试在 arch/arm64/configs/ 下寻找包含 a04 的配置文件并写入配置
          DEFCONFIG=$(find arch/arm64/configs/ -name "*a04*_defconfig" | head -n 1 | xargs basename)
          echo "使用配置文件: $DEFCONFIG"
          echo "CONFIG_KSU=y" >> arch/arm64/configs/$DEFCONFIG
          echo "CONFIG_KSU_SUKI_ULTRA=y" >> arch/arm64/configs/$DEFCONFIG
          # 三星内核通常需要关闭以下项以防冲突
          echo "CONFIG_STRICT_KERNEL_RWX=n" >> arch/arm64/configs/$DEFCONFIG

      - name: 开始编译
        run: |
          export PATH=$GITHUB_WORKSPACE/clang/bin:$PATH
          export ARCH=arm64
          export SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export CC=clang
          export CLANG_TRIPLE=aarch64-linux-gnu-
          
          # 自动获取刚才确定的 DEFCONFIG
          DEFCONFIG=$(find arch/arm64/configs/ -name "*a04*_defconfig" | head -n 1 | xargs basename)
          
          make O=out $DEFCONFIG
          make -j$(nproc --all) O=out \
                      CC=clang \
                      CLANG_TRIPLE=aarch64-linux-gnu- \
                      CROSS_COMPILE=aarch64-linux-gnu- \
                      CROSS_COMPILE_ARM32=arm-linux-gnueabi-

      - name: 打包 AnyKernel3
        run: |
          git clone https://github.com/osm0sis/AnyKernel3
          # 三星 A04 (MTK) 通常产出是 Image.gz
          if [ -f out/arch/arm64/boot/Image.gz ]; then
            cp out/arch/arm64/boot/Image.gz AnyKernel3/
          else
            cp out/arch/arm64/boot/Image AnyKernel3/
          fi
          cd AnyKernel3
          # 修改 anykernel.sh 以匹配 A04 分区 (MTK 一般是 /dev/block/by-name/boot)
          sed -i 's/block=\/dev\/block\/platform\/omap\/omap_hsmmc.0\/by-name\/boot/block=\/dev\/block\/by-name\/boot/g' anykernel.sh
          zip -r ../SukiSU-Ultra-A04-Kernel.zip *

      - name: 上传构建结果
        uses: actions/upload-artifact@v4
        with:
          name: SukiSU-Kernel-A04
          path: SukiSU-Ultra-A04-Kernel.zip
